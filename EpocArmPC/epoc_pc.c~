#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <unistd.h>
#include <unistd.h>


char szTmp[32];
sprintf(szTmp, "/proc/%d/exe", getpid());

int main()
{
   char wrkpth[32]
   setuid( 0 );
   system(sprintf(wrkpth, getpid()));

   return 0;
}
